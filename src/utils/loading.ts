import { ElLoading, LoadingOptionsResolved } from "element-plus";

// todo

// const loadFunc = (config: LoadingOptionsResolved) => {
//   const loading = ElLoading.service({ fullscreen: true, ...config });
//   return loading;
// };
